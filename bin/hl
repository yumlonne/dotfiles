#!/usr/bin/perl
use Term::ANSIColor;

my $keyword = shift @ARGV or die 'keyword not given';
my $color = color(shift @ARGV or 'yellow');
my $reset = color('reset');

while (<>) {
    s/($keyword)/$color$1$reset/g;
    print;
}
